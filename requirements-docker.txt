# ============================================================
# Dual-Engine Banking Chatbot — Docker Requirements (CPU-only)
# Used inside the Docker container (no GPU required)
# ============================================================

# PyTorch CPU-only (keeps Docker image small)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# Transformers stack
transformers>=4.38.0,<4.45.0
datasets>=2.18.0
sentence-transformers>=2.3.0
accelerate>=0.27.0
tokenizers>=0.15.0

# Vector search
faiss-cpu>=1.7.4

# Data / numerics
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0
pandas>=2.0.0

# Visualisation
matplotlib>=3.7.0
seaborn>=0.12.0

# MLflow
mlflow>=2.12.0,<3.0.0

# Protobuf pin
protobuf>=3.20.0,<5.0.0

# App
streamlit>=1.30.0
fastapi>=0.100.0
uvicorn>=0.20.0

# Config
pyyaml>=6.0

# Utilities
tqdm>=4.65.0
